# Variáveis de ambiente para a Vercel
# Copie este arquivo para .env e preencha os valores corretos

# Conexão com o banco PostgreSQL (Neon, Supabase ou outro PostgreSQL compatível)
DATABASE_URL=postgres://usuario:senha@seu-host-postgres:5432/nome-do-banco

# Configurações do servidor
PORT=5000
NODE_ENV=production